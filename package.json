{
  "name": "spotify-harness-tg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server/index.ts",
    "start": "bun run server/index.ts",
    "test": "bun test",
    "openspec:commit": "bun run scripts/openspec-commit.ts",
    "typecheck": "tsc --noEmit",
    "build:miniapp": "cd miniapp && bun run build",
    "build:dashboard": "cd dashboard && bun run build",
    "dashboard": "bun run --watch server/dashboard-server.ts"
  },
  "dependencies": {
    "grammy": "^1.28.0",
    "headroom-ai": "^0.22.4",
    "hono": "^4.6.0",
    "ytmusic-api": "^5.2.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
