{
  "name": "spotify-harness-tg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server/index.ts",
    "start": "bun run server/index.ts",
    "test": "bun test",
    "openspec:commit": "bun run scripts/openspec-commit.ts",
    "typecheck": "tsc --noEmit",
    "build:miniapp": "cd miniapp && bun run build"
  },
  "dependencies": {
    "grammy": "^1.28.0",
    "hono": "^4.6.0",
    "ytmusic-api": "^5.2.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
