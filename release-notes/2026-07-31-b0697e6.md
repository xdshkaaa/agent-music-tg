*Поиск в группах: ускорили первую отправку трека*

- Убрали лишнее перекодирование в MP3 — трек качается и уходит в чат в исходном качестве
- Обложка теперь грузится параллельно со скачиванием, а не после
- yt-dlp запоминает состояние между запросами вместо повторной работы на каждый трек
- Эффект — на первом (некэшированном) запросе трека в группе; повторные запросы того же трека и так были мгновенными
