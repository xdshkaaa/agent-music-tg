## 1. Theme tokens & shared CSS (Capability: mini-app-theme)

- [x] 1.1 In `miniapp/src/styles/glass.css`, replace `--accent: #1ed760` / `--accent-deep: #12a34a` with `--accent: #A855F7` / `--accent-deep: #7C3AED`.
- [x] 1.2 Add new tokens to `:root` in `glass.css`: `--bg: #0A0A0B`, `--card: #141416`, `--hairline: #232326`, `--muted: #8A8A8E`, `--radius-card: 20px`.
- [x] 1.3 Replace `.app-shell` background with `radial-gradient(circle at 18% 0%, rgba(168,85,247,0.22) 0%, #0A0A0B 55%)`; keep the `::after` grain overlay.
- [x] 1.4 Re-tune `:root[data-scheme="light"] .app-shell` to a soft-gray radial with the same purple accent; keep the block in place (light scheme retained).
- [x] 1.5 Update `.glass-panel` so `background: var(--card)`, `border: 1px solid var(--hairline)`, `border-radius: var(--radius-card)`; soften the `::before` tilted highlight to opacity ≤ 0.2 (do not remove).
- [x] 1.6 Update `.glass-button.primary` to white background + black bold text; in the `@media (hover: hover)` block swap the green `box-shadow` glow to a purple-tinted `rgba(168,85,247,*)` glow.
- [x] 1.7 Add a `.section-label` utility class: UPPERCASE, `letter-spacing: 0.16em`, `font-size: 12px`, `color: var(--muted)`, with `::after { content: ""; flex: 1; height: 1px; background: var(--hairline); margin-left: 10px; }` generated by making the element `display: flex; align-items: center;`.
- [x] 1.8 Add a `.chevron` tint helper (color: `var(--accent)`) so screens can place a `CaretRight` Phosphor icon with the right tint.
- [x] 1.9 In `miniapp/index.html`, add a Google Fonts `<link>` for Inter Tight and update `--font-display` in `glass.css` to `"Inter Tight", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`; remove the Manrope reference.

## 2. Mini App shell & navigation (Capability: mini-app-screens)

- [x] 2.1 In `miniapp/src/App.tsx`, extend the `Screen` union with `{ kind: "profile" }`.
- [x] 2.2 Add a sticky top-bar `<header>` rendering the `agent music` logo chip (white ring glyph + wordmark) on the left and a wallet pill (Phosphor `Wallet` icon + `{me.credits} ген`) on the right; reuse the existing `me` from `api.me()`.
- [x] 2.3 Add a floating bottom dock (`position: sticky; bottom: 8px; max-width: 480px; margin: 0 auto;`) with four Phosphor tab buttons `Создать` (`Sparkle`), `Магазин` (`Storefront`), `Профиль` (`User`), `Админ` (`Shield` — render only when `isAdmin`); each tab routes to its screen kind via `setScreen`.
- [x] 2.4 Implement the active-tab visual state: lightened pill surface + purple icon tint. `Создать` is active when `screen.kind` is `prompt|clarify|results`; `Магазин` active when `buy`; `Профиль` active when `profile`; `Админ` active when `admin`.
- [x] 2.5 Remove the previous `<nav className="nav-row">` block and its conditional `glass-button` tabs (including the previously visible `Настройки` tab).
- [x] 2.6 Render `SettingsScreen` as an additional stacked card inside `AdminScreen` so `Настройки` is reachable only from `Админ`; preserve the existing `React.lazy` boundary for `SettingsScreen` in `App.tsx`.

## 3. Mini App screens — restyle (Capability: mini-app-screens)

- [x] 3.1 `PromptScreen.tsx`: prepend a purple-nebula hero section containing UPPERCASE `СОЗДАТЬ ПЛЕЙЛИСТ` title, a `◉ AGENT MUSIC` chip, and three benefit rows (Phosphor thin purple icons + one line each: "Опишите настроение", "Моментальная генерация", "Поддержка 24/7") above the existing `<textarea>` + primary `Собрать плейлист` pill; keep `onSubmit` and `busy` behavior.
- [x] 3.2 `BuyScreen.tsx`: remove the profile block at the top of the screen (it moves to `ProfileScreen`).
- [x] 3.3 `BuyScreen.tsx`: add a search input (Phosphor `MagnifyingGlass` + `glass-input` styling, placeholder `Поиск по пакетам…`) that client-side filters `offers` by `title` case-insensitive contains.
- [x] 3.4 `BuyScreen.tsx`: add a horizontal category-pill row (`Все`, `Генерации`, `Подписка`) that filters `offers` by `grantKind` (`Все` shows all); apply STATICA pill styling on lightened active pill.
- [x] 3.5 `BuyScreen.tsx`: add a `Segmented` sort control (`Дешевле` / `Дороже` / `Популярное` = active-first) that re-orders the already-filtered offers array in-place.
- [x] 3.6 `BuyScreen.tsx`: add a `ПАКЕТЫ ────` `.section-label` above the offers list and render each offer as a tall `.glass-panel` row with left purple-ring mark tile, bold `title`, grant sub-label, right-aligned `amount asset`, and a purple `CaretRight`; clicking the row triggers the existing `buy(o.id)` flow.
- [x] 3.7 Create `miniapp/src/screens/ProfileScreen.tsx`: circular `agent music` mark chip (inline SVG, white ring on `--card`), display name (`@${username}` if set else `ID ${chatId}`), balance card (`.section-label` `АККАУНТ ─`, `Баланс`, big `me.credits`, subscription status, and a white `+ Пополнить` primary button that calls `setScreen({ kind: "buy" })` via a prop callback).
- [x] 3.8 `ProfileScreen.tsx`: render a `МОИ ПОКУПКИ ────` section listing paid invoices from `api.purchases()`; empty state shows a Phosphor `Package` thin-stroke icon + the text `Покупок пока нет` + a small dark "В магазин" pill that navigates to the `buy` screen.
- [x] 3.9 Wire `ProfileScreen` into `App.tsx` at `screen.kind === "profile"` and ensure the existing `api.me()` result is passed as props (`credits`, `subscriptionUntil`, `username`, `chatId`).
- [x] 3.10 `ResultsScreen.tsx`: wrap each track row in the new dark-card `.glass-panel` skin with hairline separators; re-style the `Новый плейлист` button as a dark secondary pill (`glass-button` without `primary`); keep the `onNewPrompt` callback.
- [x] 3.11 `ClarifyScreen.tsx`: re-style each option button as a dark pill row with a purple `CaretRight` chevron on the right; keep `onAnswer` / `busy` behavior.
- [x] 3.12 `AdminScreen.tsx`: render `SettingsScreen` as a stacked card inside it (per task 2.6) above the existing admin cards; ensure no duplicate `SettingsScreen` mount elsewhere.
- [x] 3.13 `AdminScreen.tsx` / `SettingsScreen.tsx`: confirm they inherit the new dark-card skin from CSS only — no_per-screen structural rewrite; verify `Segmented` and `GlassPanel` still render correctly.
- [x] 3.14 (Optional) `BuyScreen.tsx`: add a localStorage-diff "checkmark ring" toast — on mount compare `paidInvoices.length` to `localStorage["am_last_paid_count"]`; if greater, render a one-shot purple-glow checkmark-ring toast that auto-dismisses in 2.5s and updates the stored count. Skip if the optional flag is dropped.

## 4. Bot premium-emoji resolver (Capability: bot-premium-emoji)

- [x] 4.1 Add `emojiStickerSet: process.env.EMOJI_STICKER_SET ?? ""` to `server/env.ts` and an optional `EMOJI_STICKER_SET=` line with a comment to `.env.example`.
- [x] 4.2 Create `server/bot/emoji-symbols.json` mapping `custom_emoji_id → symbol` (start empty / placeholder; the live IDs come from the bot's own sticker set once registered).
- [x] 4.3 Create `server/bot/emoji.ts`: export `loadCustomEmojis(bot)`, `btn(label, symbol)`, `accent(symbol)`, and `fallbackSymbol(symbol)`.
- [x] 4.4 Implement `loadCustomEmojis(bot)`: when `env.emojiStickerSet` is set, call `bot.api.getStickerSet(env.emojiStickerSet)` once and build a `symbol → custom_emoji_id` map by pairing sticker `custom_emoji_id` with the symbol from `emoji-symbols.json`; cache the map for the process lifetime; silently skip entries without a known mapping.
- [x] 4.5 Implement a static unicode fallback table covering at least `info`, `diamond`, `music`, `stats`, `package`, `broadcast`, `gear`, `check`, `profile`, `wallet`, `plus`, `ruler`, `sparkle` (`info: "ℹ️"`, `diamond: "💎"`, `music: "🎵"`, `stats: "📊"`, `package: "📦"`, `broadcast: "📨"`, `gear: "⚙️"`, `check: "✅"`, `profile: "👤"`, `wallet: "💳"`, `plus: "➕"`, `ruler: "📏"`, `sparkle: "⚡"`).
- [x] 4.6 Implement `btn(label, symbol)`: return `label` prefixed by the `<custom_emoji_id:ID>` token if `symbol` is in the map, otherwise prefix with `fallbackSymbol(symbol) + " "`.
- [x] 4.7 Implement `accent(symbol)`: return `<tg-emoji emoji-id="<ID>">…</tg-emoji>` if `symbol` is mapped; otherwise return `fallbackSymbol(symbol)`. The wrapped visible text inside the `<tg-emoji>` tag is the unicode fallback glyph (Telegram replaces it with the sticker image at render).
- [x] 4.8 Call `loadCustomEmojis(bot)` once at bot startup in `server/bot/index.ts` (after `createBot`, before `bot.start()`).
- [x] 4.9 Add a unit test `server/bot/emoji.test.ts` covering: unicode fallback when `EMOJI_STICKER_SET` unset, `btn` prefixes correctly, unknown symbol falls back to its unicode fallback, `accent` returns a `<tg-emoji>` tag (or unicode) depending on the map.

## 5. Bot UI copy & inline keyboards (Capability: bot-ui-copy)

- [x] 5.1 `server/bot/index.ts` `/start`: rewrite the reply text to begin with `accent("info") + " AGENT MUSIC"` and three bullet lines ("Сгенерируй плейлист", "Купи доступ", "Поддержка 24/7") before the existing `shop.aboutText` and command list; send with `parse_mode: "HTML"`; keep `InlineKeyboard().webApp(btn("Открыть приложение", "sparkle"), env.publicOrigin)`.
- [x] 5.2 `server/bot/index.ts` `/app`: prefix the reply with `accent("info")`; attach the same webApp keyboard (label via `btn("Открыть приложение", "sparkle")`); send with `parse_mode: "HTML"`.
- [x] 5.3 `server/bot/index.ts` `/about`: prefix with `accent("info")`; render with `parse_mode: "HTML"`; keep the `getShopSettings` shopName + aboutText body.
- [x] 5.4 `server/bot/shop.ts` `purchasePromptText()`: return `accent("ruler") + " ДОСТУП ────\nЧтобы генерировать плейлисты, нужен доступ. Выберите пакет:"`; verify the consumer `sendOffers` sends with `parse_mode: "HTML"`.
- [x] 5.5 `server/bot/shop.ts` `offersKeyboard`: prefix each offer label via `btn(offerLabel(o), o.grantKind === "subscription" ? "ruler" : "music")`; keep `buy:<id>` callback data byte-identical.
- [x] 5.6 `server/bot/shop.ts` `/profile`: rewrite the reply header to `accent("profile") + " ПРОФИЛЬ ─"` and the per-line emoji via `accent` / `fallbackSymbol`; send with `parse_mode: "HTML"`. The credits / subscription / purchase-list data values must remain identical.
- [x] 5.7 `server/bot/admin-panel.ts` `menuKeyboard`: prefix each menu row label via `btn` (`📊 Статистика` → `btn("Статистика", "stats")`, etc.); keep `admin:stats` / `admin:offers` / `admin:broadcast` / `admin:settings` callback data byte-identical.
- [x] 5.8 `server/bot/admin-panel.ts` `showOffers`: prefix each offer row label via `btn(offerLabel-like-text, "package")`; keep `admin:offer:toggle:<id>` / `admin:offer:del:<id>` callback data byte-identical.
- [x] 5.9 `server/bot/admin-panel.ts`: leave all `answerCallbackQuery({ text })` strings as plain unicode emoji (no `<tg-emoji>` / `custom_emoji_id` tokens); document at the call site with a one-line comment that `answerCallbackQuery.text` does not support HTML/custom emoji.
- [x] 5.10 `server/bot/admin-panel.ts`: leave `handleAdminText` FSM and the `/provider`, `/backend` replies byte-identical (no premium-emoji or `<tg-emoji>` tag).
- [x] 5.11 Add / update server tests (`server/bot/shop.test.ts` if absent) verifying: `offersKeyboard` callback data matches `/^buy:\d+$/`, `purchasePromptText` starts with the ДОСТУП header (when emoji sticker set unset, prefix is `📏`), and `/profile` heading contains `ПРОФИЛЬ`.

## 6. API: additive `/api/me` fields (Capability: mini-app-screens)

- [x] 6.1 In the `/api/me` handler (search `server/api/` for the `me` route), extend the response body with `username?: string` (from the `users.username` column) and `chatId: number` (from the authenticated caller); keep `isAdmin`, `credits`, `subscriptionUntil` field names and positions intact.
- [x] 6.2 Extend `miniapp/src/lib/api.ts` `MeResponse` type with `username?: string` and `chatId: number` (additive); do not break callers that ignore these fields.
- [x] 6.3 Add a server test that the `/api/me` response for an allowed chat includes `chatId` and `username` (when set) and still includes the previous fields.

## 7. Verification

- [x] 7.1 `bun run typecheck` (root `tsc --noEmit`) passes.
- [x] 7.2 `cd miniapp && bun run build` (which runs `tsc --noEmit && vite build`) succeeds.
- [x] 7.3 `bun test` (server tests) passes — including the existing shop/admin/payments tests and the new emoji / shop / me tests.
- [x] 7.4 Manual (read this task; not executed as part of the change): run `bun run dev`, log into an allowlist chat, exercise `/start`, `/app`, `/about`, `/buy`, `/profile`, `/admin` (admin), open the Mini App, exercise `Создать` → submit a prompt → go through `clarify` → `results`; tap `Магазин` → search/filter/sort → `buy`; tap `Профиль` → see balance and `+ Пополнить` → `В магазин`; tap `Админ` → stats / offers / broadcast / shop settings / `Настройки` (provider/backend). Confirm premium-emoji glyphs render when `EMOJI_STICKER_SET` set, otherwise plain unicode fallback renders.