# Tasks

## 1. Аудит покупки end-to-end (тесты как контракт)

- [x] 1.1 Интеграционный тест: crypto-покупка кредитного оффера — `POST /api/invoices` → подписанный webhook → баланс в `/api/me` вырос, `hasAccess` true
- [x] 1.2 Интеграционный тест: crypto-покупка подписочного оффера — продление `subscriptionUntil`, стек с остатком
- [x] 1.3 Интеграционный тест: stars-покупка (`fulfillStarsPayment`) — кредиты и подписка, идемпотентность дубликатов `successful_payment`
- [x] 1.4 Тест поллера: paid-инвойс без webhook исполняется через `poller.ts` ровно один раз (webhook + поллер вместе — один грант)
- [x] 1.5 Тест: неактивный/удалённый оффер → инвойс не создаётся, 4xx
- [x] 1.6 Починить все разрывы, найденные тестами 1.1–1.5 (разрывов нет; поллер получил инъекцию `fetchInvoices` для тестируемости)

## 2. Трата генераций

- [x] 2.1 Тесты `entitlements`/`run-generation`: один кредит за успех (trial→paid порядок), clarify бесплатен, ошибка бесплатна, двойного списания нет
- [x] 2.2 Тест SSE-пути: `/generate/stream` (+ resume/extend stream) списывает ровно один кредит при успехе
- [x] 2.3 Тест: нулевой баланс на каждом пути → `needs_purchase`, LLM не вызывается
- [x] 2.4 Починить найденные разрывы списания (разрывов нет — все пути идут через run-generation)

## 3. Подписка

- [x] 3.1 Тесты: активная подписка — генерация без списания кредитов; истёкшая — фолбэк на кредиты/`needs_purchase`
- [x] 3.2 Проверить отображение подписки в `/api/me`, миниапп-профиле и боте (`/profile`); починить, если срок/статус не показывается

## 4. Почасовой рейт-лимит подписки

- [x] 4.1 `server/env.ts`: `SUBSCRIPTION_HOURLY_LIMIT` (int, default 25, 0 = off)
- [x] 4.2 `generations-store.ts`: `countRecentGenerations(db, chatId, sinceSeconds)` + `oldestGenerationInWindow` для расчёта `retryAt`
- [x] 4.3 `entitlements.ts`: `checkSubscriptionRateLimit(db, chatId)` — применяется только при доступе по подписке (нет кредитов и активного trial), не для админов, не при `paymentsEnabled=false`
- [x] 4.4 `run-generation.ts`: исход `{ status: "rate_limited"; retryAt }` во всех трёх функциях до LLM-вызова
- [x] 4.5 `routes.ts`: JSON-роуты → HTTP 429 c `retryAt`; SSE — терминальный кадр `rate_limited` и закрытие стрима
- [x] 4.6 Бот: не требуется — генерация только в Mini App (server/bot/index.ts:172); /credits починен для trial-пользователей «лимит генераций исчерпан, снова доступно в HH:MM»
- [x] 4.7 Миниапп: `lib/api.ts` тип исхода, `PromptScreen`/`ClarifyScreen` — сообщение с временем, без кнопки покупки
- [x] 4.8 Тесты рейт-лимита: под лимитом / на лимите / скользящее окно / limit=0 / кредитный пользователь не троттлится / админ не троттлится

## 5. Верификация

- [x] 5.1 `bun test` — все серверные тесты зелёные
- [x] 5.2 `bun run typecheck` и `bun run build:miniapp`
- [ ] 5.3 Ручная проверка на VPS после деплоя: покупка тестового оффера, списание, лимит (по договорённости с пользователем)
